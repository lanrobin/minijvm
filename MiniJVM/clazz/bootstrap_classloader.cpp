#include "bootstrap_classloader.h"

shared_ptr<VMClass> BootstrapClassLoader::loadClass(const wstring& className) {
	return nullptr;
}

shared_ptr<VMClass> BootstrapClassLoader::loadClass(shared_ptr<Buffer> buf) {
	return nullptr;
}